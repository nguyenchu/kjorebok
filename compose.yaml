services:
  postgres:
    image: postgres:16
    container_name: kjorebok-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kjorebok
    ports:
      - "3022:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
